{
  "mcpServers": {
    "arxiv": {
      "command": "uvx",
      "args": ["arxiv-mcp-server"],
      "description": "Search, download, and read arxiv papers (optional — skill degrades gracefully without it)"
    },
    "arxiv-latex": {
      "command": "uvx",
      "args": ["arxiv-latex-mcp-server"],
      "description": "Access raw LaTeX source of arxiv papers (optional)"
    }
  }
}
